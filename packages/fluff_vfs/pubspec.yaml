name: fluff_vfs
description: >-
  Virtual file-system abstraction for Fluff. Defines FsNode, FsProvider
  and ships an in-memory provider for tests and web mocks.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  meta: ^1.16.0
  archive: ^4.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
