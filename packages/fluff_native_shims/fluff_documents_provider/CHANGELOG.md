## 0.0.1

- Initial scaffold (Phase 6, not yet wired). Defines the public Dart
  surface (`FluffDocumentsProvider.serve`, `DocumentsRoot`) and the
  Android plugin/`DocumentsProvider` skeleton. No functional bridge
  yet.
