name: fluff_vault
description: >-
  Encrypted, streaming-AEAD vault on top of any FsProvider.
  XChaCha20-Poly1305 + Argon2id + HKDF (pure Dart). Header spec
  frozen in PLAN.md §6.4.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  cryptography: ^2.7.0
  meta: ^1.16.0
  fluff_vfs:
    path: ../fluff_vfs

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
