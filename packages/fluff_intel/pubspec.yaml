name: fluff_intel
description: >
  Mock OCR, semantic-search index, and AI rename / organise scaffolding
  for Fluff. Web slice — real ML Kit + Gemini wired in Phase 8.1.
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.7.0

dependencies:
  meta: ^1.16.0
  fluff_vfs:
    path: ../fluff_vfs

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
