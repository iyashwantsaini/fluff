# Screenshots

Auto-generated per iteration by the **build-tier** skill. Layout:

```
docs/screenshots/
  phase-1/
    browse@<W>x<H>.png
    ...
  phase-2/
    ...
```

`W` × `H` is the **detected** Simple Browser viewport — never
hard-coded. See [`../DESIGN.md`](../DESIGN.md) "Iteration log" for
the review of each batch.
